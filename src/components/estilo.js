import React from "react";
import { StyleSheet, Text, View } from "react-native";

const estilo = StyleSheet.create(
    {
        fundo:{
            flexGrow: 1,
            justifyContent: 'center',
            alignItems: 'center',
            backgroundColor: '#e3c4cd',
        }
    }
)

export default estilo;